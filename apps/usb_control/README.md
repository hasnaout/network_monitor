# USB Control

Enterprise-safe USB monitoring module.

- The agent sends a periodic snapshot of currently connected removable drives.
- The server stores connected/disconnected state, history, and alerts.
- This module does not disable USB devices on client machines.
